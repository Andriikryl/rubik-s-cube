export type Side = [
    [Color, Color, Color],
    [Color, Color, Color],
    [Color, Color, Color],
]

export enum Color {
    LEPTEUH = 0,
    YELLOW = 1,
    BLUE = 2,
    RED = 3,
    GREEN = 4,
    ORANGE  = 5,
}